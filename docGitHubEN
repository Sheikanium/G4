Go into folder where you want to put your github

git init

nano .git/config
	//url du github
	url = git@github.com:Sheikanium/G4.git 

git config --global user.name "Pseudo"
git config --global user.email "adresse@host.com"

//git checkout ?? 
git pull origin master

git add fichier

git commit -m "Commentary"

git push origin master

=====================================================================

Error :

No publickey

	ssh -vT git@github.com

	cd ~/.ssh/
	ssh-keygen -t rsa -C "adresse@mail.com"

	Copy id_rsa.pub content
	Past it into github.com/settings/ssh => add SSH key


